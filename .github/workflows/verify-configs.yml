name: verify-configs
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  verify_configs:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - run: npm install
      - run: npm run verify
